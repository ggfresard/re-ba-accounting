// Definir los endpoints de la api que usará la aplicación.
export enum Endpoints {
  Partner = 'partners/',
  UserAuth = 'auth/user',
  Login = 'auth/login',
  Logout = 'auth/logout',
  Project = 'projects/',
  Flow = 'flows/',
  FlowConfirm = 'flows/confirm/',
  Expense = 'expenses/'
}
